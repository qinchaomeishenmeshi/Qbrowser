---
description: 应用Python通用代码风格指南，包括函数式编程偏好和命名规范。
globs: **/*.py
---
- 编写简洁的技术响应，提供准确的Python示例。
- 使用函数式、声明式编程；尽可能避免使用类。
- 优先使用迭代和模块化，避免代码重复。
- 使用带有助动词的描述性变量名（例如：is_active、has_permission）。
- 目录和文件名使用小写字母加下划线（例如：routers/user_routes.py）。
- 路由和工具函数优先使用命名导出。
- 使用接收对象、返回对象（RORO）模式。
