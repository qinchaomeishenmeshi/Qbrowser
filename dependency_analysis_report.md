# pyproject.toml 依赖配置分析报告

## 📋 检查概述

本报告分析了 `pyproject.toml` 文件中的依赖配置，识别了潜在问题并提供了优化建议。

## 🔍 发现的问题

### 1. Python版本要求过于严格

**问题**: `requires-python = ">=3.12"`
- 当前要求Python 3.12+，这会限制很多用户的使用
- 大多数生产环境可能还在使用Python 3.8-3.11

**建议**: 降低到 `requires-python = ">=3.8"`
- Python 3.8是目前广泛支持的最低版本
- 提高项目的兼容性和可用性

### 2. psutil版本问题

**问题**: `psutil~=7.0.0`
- psutil 7.0.0是最新版本，但在某些系统上可能存在兼容性问题
- 波浪号约束可能导致意外的版本升级

**建议**: 使用 `psutil>=5.9.0,<6.1.0`
- 使用经过充分测试的稳定版本
- 避免最新版本可能带来的不稳定性

### 3. PyQt6版本兼容性

**问题**: `pyqt6==6.9.0`
- PyQt6 6.9.0在某些macOS版本上可能存在问题
- 固定版本可能导致兼容性问题

**建议**: 使用 `pyqt6>=6.7.0,<6.10.0`
- 允许在稳定版本范围内灵活选择
- 避免最新版本的潜在问题

### 4. 版本约束方式不一致

**问题**: 混合使用了多种版本约束方式
- 有些使用波浪号 `~=`
- 有些使用精确版本 `==`
- 有些使用范围约束 `>=,<`

**建议**: 统一使用范围约束
- 对于核心依赖使用范围约束 `>=x.y.z,<x+1.0.0`
- 对于工具类依赖可以使用更宽松的约束

### 5. 缺少测试和文档相关依赖

**问题**: 可选依赖中缺少测试和文档生成工具

**建议**: 添加以下可选依赖组
```toml
[project.optional-dependencies]
test = [
    "pytest>=7.4.3",
    "pytest-asyncio>=0.21.1",
    "pytest-cov>=4.1.0",
]

docs = [
    "sphinx>=7.0.0",
    "sphinx-rtd-theme>=1.3.0",
]
```

## ✅ 配置良好的方面

1. **依赖分类合理**: 将可选依赖按功能分组（数据分析、图像处理、开发工具）
2. **使用国内镜像**: 配置了清华大学镜像源，提高下载速度
3. **构建系统配置**: 正确配置了setuptools作为构建后端
4. **包排除配置**: 合理排除了测试、文档等非发布内容

## 🔧 优化建议

### 立即修复的问题

1. **降低Python版本要求**
   ```toml
   requires-python = ">=3.8"
   ```

2. **修复psutil版本**
   ```toml
   "psutil>=5.9.0,<6.1.0"
   ```

3. **优化PyQt6版本约束**
   ```toml
   "pyqt6>=6.7.0,<6.10.0"
   "pyqt6-qt6>=6.7.0,<6.10.0"
   "pyqt6-webengine>=6.7.0,<6.10.0"
   ```

### 长期优化建议

1. **添加代码质量工具配置**
   - 添加black、isort、mypy、pytest配置
   - 统一代码风格和质量标准

2. **完善可选依赖**
   - 添加test、docs依赖组
   - 细化dev依赖，添加更多开发工具

3. **版本约束策略**
   - 对核心功能依赖使用严格约束
   - 对工具类依赖使用宽松约束
   - 定期更新和测试依赖版本

## 📁 生成的文件

1. **pyproject_optimized.toml**: 优化后的完整配置文件
2. **dependency_analysis_report.md**: 本分析报告
3. **check_dependencies.py**: 依赖检查脚本

## 🚀 下一步行动

1. **备份当前配置**
   ```bash
   cp pyproject.toml pyproject.toml.backup
   ```

2. **应用优化配置**
   ```bash
   cp pyproject_optimized.toml pyproject.toml
   ```

3. **重新安装依赖**
   ```bash
   uv sync
   ```

4. **测试应用程序**
   ```bash
   python app.py
   ```

5. **运行测试（如果有）**
   ```bash
   uv run pytest
   ```

## ⚠️ 注意事项

1. **兼容性测试**: 修改依赖版本后，需要充分测试应用程序的所有功能
2. **渐进式更新**: 建议逐步应用修改，而不是一次性全部更改
3. **版本锁定**: 在生产环境中考虑使用uv.lock文件锁定具体版本
4. **持续监控**: 定期检查依赖的安全更新和兼容性

## 📊 风险评估

| 修改项目 | 风险等级 | 影响范围 | 建议 |
|---------|---------|---------|------|
| Python版本要求 | 低 | 兼容性提升 | 立即应用 |
| psutil版本 | 中 | 系统监控功能 | 测试后应用 |
| PyQt6版本 | 中 | GUI界面 | 谨慎测试 |
| 其他依赖优化 | 低 | 整体稳定性 | 逐步应用 |

---

**生成时间**: $(date)
**检查工具**: check_dependencies.py
**项目**: qw-browser v0.1.0